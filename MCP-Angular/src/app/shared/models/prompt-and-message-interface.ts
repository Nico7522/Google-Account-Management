/**
 * Interface for the prompt and message.
 * @param prompt - The prompt to send to the server.
 * @param message - The message to display to the user.
 */
export interface PromptAndMessage {
  prompt: string;
  message: string;
}
